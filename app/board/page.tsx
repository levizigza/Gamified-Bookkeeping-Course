import { LedgerQuestBoard } from "@/components/board/LedgerQuestBoard";

export const metadata = {
  title: "Ledger Quest Board — Digital Bookkeeping Race",
  description:
    "Race through four unlockable bookkeeping worlds, complete missions, earn stars, and reach accountant-ready books.",
};

export default function BoardPage() {
  return <LedgerQuestBoard />;
}
